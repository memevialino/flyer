# Web source adapters

Add permitted public-web adapters here. Each must normalize to `{origin,destination,airline,flightNumber,aircraft,source,checkedAt}`. Missing evidence must remain UNKNOWN; never infer false from missing data.
