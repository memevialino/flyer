export const Verdict={VALID:"VALID",INVALID:"INVALID",UNKNOWN:"UNKNOWN"};export function validateRound(round){if(!Array.isArray(round.options)||round.options.length!==5)return{playable:false,reason:"OPTION_COUNT"};const valid=round.options.filter(o=>o.verdict===Verdict.VALID);const unknown=round.options.filter(o=>o.verdict===Verdict.UNKNOWN);if(valid.length!==1)return{playable:false,reason:"NOT_EXACTLY_ONE_VALID"};if(unknown.length)return{playable:false,reason:"UNKNOWN_PRESENT"};return{playable:true,correctOptionId:valid[0].id}}